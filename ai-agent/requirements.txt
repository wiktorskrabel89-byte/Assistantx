# ── Jarvis AI-Agent Sidecar — Python dependencies ───────────────────────────
# Core async server
websockets>=12.0

# Wake word detection
openwakeword>=0.6.0

# Speech-to-text (Parakeet ONNX local CPU path)
onnxruntime>=1.18.0
huggingface_hub>=0.23.0

# Text-to-speech (Kokoro local CPU path)
kokoro>=0.9.4

# NLP / intent parsing (legacy local fallback only)
spacy>=3.7.0
# English model installed separately: python -m spacy download en_core_web_sm

# Audio I/O
sounddevice>=0.4.7
numpy>=1.26.0

# Voice Activity Detection (primary local gate before remote STT)
silero-vad>=5.1.2
webrtcvad>=2.0.10
rapidfuzz>=3.14.5

# Local memory / retrieval
lancedb>=0.11.0
sentence-transformers>=3.0.0
pyarrow>=16.0.0
pathspec>=0.12.0
psutil>=5.9.0

# Cloud AI providers (cloud engine mode: STT + TTS via OpenAI API)
openai>=1.30.0
