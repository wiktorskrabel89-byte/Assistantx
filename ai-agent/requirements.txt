# ── Jarvis AI-Agent Sidecar — Python dependencies ───────────────────────────
# Core async server
websockets>=12.0

# Wake word detection
openwakeword>=0.6.0

# Speech-to-text
faster-whisper>=1.0.3

# Text-to-speech (Piper)
piper-tts>=1.2.0

# NLP / intent parsing
spacy>=3.7.0
# English model installed separately: python -m spacy download en_core_web_sm

# Audio I/O
sounddevice>=0.4.7
numpy>=1.26.0

# Voice Activity Detection (helps gate Whisper invocations)
webrtcvad>=2.0.10
